import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  contentNotExpanded: {
    height: 0,
    padding: 0,
    paddingBottom: '0 !important',
  },
  header: {
    cursor: 'pointer',
  },
}));
